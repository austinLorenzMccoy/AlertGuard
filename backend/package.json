{
  "name": "alertguard-backend",
  "version": "1.0.0",
  "description": "AlertGuard Supabase backend: migrations, RLS, and Edge Function logic.",
  "type": "module",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@stacks/encryption": "^6.17.0",
    "@stacks/transactions": "^6.17.0"
  },
  "devDependencies": {
    "@types/node": "^22.15.18",
    "@vitest/coverage-v8": "^3.1.3",
    "typescript": "^5.6.3",
    "vitest": "^3.1.3"
  }
}
